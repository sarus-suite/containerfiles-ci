name: Release of build-slurm opensuse container multi-arch

on:
  workflow_dispatch:
  push:
    paths:
      - 'build/slurm/opensuse/Containerfile'

permissions:
  contents: write
  packages: write
  actions: read

env:
  REGISTRY: ghcr.io
  REPO: ghcr.io/${{ github.repository }}/build-slurm


jobs:
  build:
    strategy:
      matrix:
        os_version: [ 15.5, 15.6 ]
        slurm_version: [ 24.05.3-1, 25.05.4-1 ] 
        arch: [ amd64, arm64 ]
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        id: buildx

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push single-arch image
        run: |
          TAG="${{ matrix.slurm_version }}-opensuse-${{ matrix.os_version }}-${{ matrix.arch }}"
          CTX="build/slurm/opensuse"
          docker buildx build \
            --builder "${{ steps.buildx.outputs.name }}" \
            --platform linux/${{ matrix.arch }} \
            --file build/slurm/opensuse/Containerfile \
            --build-arg os_version=${{ matrix.os_version }} \
            --build-arg slurm_version=${{ matrix.slurm_version }} \
            --tag  "${{ env.REPO }}:${{ TAG }}" \
            --push \
            "${CTX}"

# Multi-arch manifest that references images for both architectures
  manifest:
    name: Create multi-arch manifest
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os_version: [ 15.5, 15.6 ]
        slurm_version: [ 24.05.3-1, 25.05.4-1 ] 

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify single-arch images exist
        run: |
          set -euo pipefail

          IMAGES=(
            "${{ env.REPO }}:build-amd64"
            "${{ env.REPO }}:build-arm64"
          )

          for img in "${IMAGES[@]}"; do
            echo "Inspecting $img ..."
            if ! docker buildx imagetools inspect "$img" > /dev/null; then
              echo "::error file=manifest.yml::Image not found: $img"
              exit 1
            fi
          done

      - name: Create & push multi-arch manifest
        run: |
          apt-get update && apt-get install -y --no-install-recommends skopeo jq
          TAG_PREFIX="${{ matrix.slurm_version }}-opensuse-${{ matrix.os_version }}"
          TAG=$(skopeo inspect --config docker://${{ env.REPO }}:${{ TAG_PREFIX }}-amd64 | jq -r '.config.Labels["org.opencontainers.image.version"]')
          docker buildx imagetools create \
            --tag "${{ env.REPO }}:${TAG}" \
            "${{ env.REPO }}:${{ TAG_PREFIX }}-amd64" \
            "${{ env.REPO }}:${{ TAG_PREFIX }}-arm64"

